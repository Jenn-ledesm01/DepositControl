@model DepositControl.Bussines.DeliveryNote

@{
    Layout = "~/Views/Shared/_LayoutBcri.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Eliminar Remito</title>
</head>
<body>
    @if (TempData["Alert"] != null)
    {
        <div class="alert alert-dismissable alert-danger">
            <button type="button" class="close" data-dismiss="alert">×</button>
            <strong>Error!</strong> @TempData["Alert"]
        </div>
    }
    <div style="text-align: center;">
        <h3>¿Está seguro que desea eliminar el remito?</h3>
        <div>
            @using (Html.BeginForm("Delete", "DeliveryNote", new { id = Model.Id }, FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div class="form-actions no-color">
                    <input type="submit" value="Eliminar" class="btn btn-danger" /> |
                    @Html.ActionLink("Volver a la Grilla", "Index", "DeliveryNote")
                </div>
            }
        </div>
    </div>
</body>
</html>