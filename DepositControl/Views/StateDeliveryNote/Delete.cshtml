@model DepositControl.Bussines.StateDeliveryNote

@{
    Layout = "~/Views/Shared/_LayoutBcri.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Eliminar Estado de Remito</title>
</head>
<body>
    <div style="text-align: center;">
        <h3>¿Está seguro que desea eliminar el estado de remito: @Model.Name?</h3>
        <div>
            @using (Html.BeginForm("Delete", "StateDeliveryNote", new { id = Model.Id }, FormMethod.Post))
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Eliminar" class="btn btn-danger" /> |
                    @Html.ActionLink("Volver a la Grilla", "Index", "StateDeliveryNote")
                </div>
            }
        </div>
    </div>
</body>
</html>